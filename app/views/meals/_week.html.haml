.row-fluid
  - week.each do |date|
    - klass = 'today' if date == @today
    .date.span1{ :class => klass }
      - meal = @meals[date]
      .row-fluid
        = nice_print(date)
      .row-fluid
        - if meal
          = meal.name
        - else
          = link_to 'New', new_meal_path
